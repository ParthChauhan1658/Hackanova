[pytest]
asyncio_mode = auto
pythonpath = .
testpaths = tests
markers =
    slow: marks tests as slow integration benchmarks (deselect with -m "not slow")
    integration: marks tests as integration tests
filterwarnings =
    ignore::DeprecationWarning
log_cli = true
log_cli_level = INFO
